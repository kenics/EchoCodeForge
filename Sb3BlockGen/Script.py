class Script:    def __init__(self, starting_block=None, sprite=None):        self.starting_block = starting_block        self.sprite = sprite        self.blocks = []  # 謇句虚霑ｽ蜉縺輔ｌ縺溘ヶ繝ｭ繝・け繧剃ｿ晄戟        if starting_block:            starting_block.topLevel = True            self.blocks.append(starting_block    def add_block(self, block):        """蜿､縺・set_next 繝√ぉ繝ｼ繝ｳ蠖｢蠑上↓繧ょｯｾ蠢懷庄閭ｽ"""        if not self.starting_block:            self.starting_block = block            block.topLevel = True        else:            current = self.starting_block            while current.next:                current = current.next            current.set_next(block)    def collect_blocks(self):        """project.json 縺ｮ blocks 繝輔ぅ繝ｼ繝ｫ繝牙・蜉帷畑"""        return self.blocks  # append 縺輔ｌ縺溘ヶ繝ｭ繝・け繧偵◎縺ｮ縺ｾ縺ｾ霑斐☆    def link_blocks(self):        #        script.blocks 蜀・・繝悶Ο繝・け繧帝・分縺ｫ謗･邯壹☆繧具ｼ・ext / parent 繧定・蜍戊ｨｭ螳夲ｼ・        if not self.blocks:            return        self.blocks[0].topLevel = True  # 譛蛻昴・繝悶Ο繝・け縺ｮ縺ｿ繝医ャ繝励Ξ繝吶Ν        for i in range(len(self.blocks) - 1):            self.blocks[i].set_next(self.blocks[i + 1])